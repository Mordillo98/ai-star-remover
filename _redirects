# Cloudflare Pages clean URLs (pretty-URL fallback). Pretty URLs are on by default,
# these explicit rewrites ensure /privacy etc. serve /privacy.html on any host.
/privacy        /privacy.html        200
/terms          /terms.html          200
/about          /about.html          200
/contact        /contact.html        200
